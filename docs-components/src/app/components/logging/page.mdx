export const metadata = {
  title: 'Logging & Monitoring',
  description: 'Monitor your bots. Logging, screenshots, alerts, and audit trails.',
}

export const sections = [
  { title: 'Log Message', id: 'logging-log' },
  { title: 'Log Screenshot', id: 'logging-screenshot' },
  { title: 'Log Metric', id: 'logging-metric' },
  { title: 'Start Timer', id: 'logging-timer_start' },
  { title: 'Stop Timer', id: 'logging-timer_stop' },
  { title: 'Send Notification', id: 'logging-notification' },
  { title: 'Audit Log', id: 'logging-audit' },
  { title: 'Export Logs', id: 'logging-export' },
]

Monitor your bots. Logging, screenshots, alerts, and audit trails. {{ className: 'lead' }}

---

<NodeHeader id="logging-log" icon="ScrollText" label="Log Message" description="Write log message" bgColor="#f1f5f9" textColor="#475569" />

<Properties>
  <Property name="type" type="string">
    `logging.log`
  </Property>
</Properties>

**Configuration:**
- **message** (`textarea`, required) — Log message
- **level** (`select`, default: `INFO`) — Log level:
  - `DEBUG` — DEBUG
  - `INFO` — INFO
  - `WARN` — WARN
  - `ERROR` — ERROR

**Output:**
- **message** (`string`) — The logged message
- **level** (`string`) — Log level used
- **timestamp** (`string`) — Timestamp when logged

---

<NodeHeader id="logging-screenshot" icon="Camera" label="Log Screenshot" description="Capture screenshot to log" bgColor="#f1f5f9" textColor="#475569" />

<Properties>
  <Property name="type" type="string">
    `logging.screenshot`
  </Property>
</Properties>

**Configuration:**
- **description** (`text`) — Screenshot description

---

<NodeHeader id="logging-metric" icon="Variable" label="Log Metric" description="Record numeric metric" bgColor="#f1f5f9" textColor="#475569" />

<Properties>
  <Property name="type" type="string">
    `logging.metric`
  </Property>
</Properties>

**Configuration:**
- **name** (`text`, required) — Metric name
- **value** (`number`, required) — Metric value
- **unit** (`text`) — Unit of measurement

---

<NodeHeader id="logging-timer_start" icon="Timer" label="Start Timer" description="Start performance timer" bgColor="#f1f5f9" textColor="#475569" />

<Properties>
  <Property name="type" type="string">
    `logging.timer_start`
  </Property>
</Properties>

**Configuration:**
- **name** (`text`, required) — Timer name

---

<NodeHeader id="logging-timer_stop" icon="TimerOff" label="Stop Timer" description="Stop and log timer" bgColor="#f1f5f9" textColor="#475569" />

<Properties>
  <Property name="type" type="string">
    `logging.timer_stop`
  </Property>
</Properties>

**Configuration:**
- **name** (`text`, required) — Timer name to stop

---

<NodeHeader id="logging-notification" icon="Bell" label="Send Notification" description="Send alert notification" bgColor="#f1f5f9" textColor="#475569" />

<Properties>
  <Property name="type" type="string">
    `logging.notification`
  </Property>
</Properties>

**Configuration:**
- **channel** (`select`, default: `email`) — Notification channel:
  - `email` — Email
  - `slack` — Slack
  - `teams` — Teams
  - `webhook` — Webhook
- **message** (`textarea`, required) — Notification message

---

<NodeHeader id="logging-audit" icon="ClipboardList" label="Audit Log" description="Create audit entry" bgColor="#f1f5f9" textColor="#475569" />

<Properties>
  <Property name="type" type="string">
    `logging.audit`
  </Property>
</Properties>

**Configuration:**
- **action** (`text`, required) — Action performed
- **details** (`textarea`) — Additional details as JSON

---

<NodeHeader id="logging-export" icon="FileDown" label="Export Logs" description="Export run logs" bgColor="#f1f5f9" textColor="#475569" />

<Properties>
  <Property name="type" type="string">
    `logging.export`
  </Property>
</Properties>

**Configuration:**
- **path** (`text`, required) — Output path
- **format** (`select`, default: `json`) — Export format:
  - `json` — JSON
  - `csv` — CSV
  - `html` — HTML

---

