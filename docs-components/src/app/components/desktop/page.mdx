export const metadata = {
  title: 'Desktop Automation',
  description: 'Components for desktop application automation. Control Windows applications, manage windows, simulate keyboard and mouse.',
}

export const sections = [
  { title: 'Open Application', id: 'desktop-open_app' },
  { title: 'Desktop Click', id: 'desktop-click' },
  { title: 'Type Text', id: 'desktop-type_text' },
  { title: 'Send Hotkey', id: 'desktop-hotkey' },
  { title: 'Get Window', id: 'desktop-get_window' },
  { title: 'Minimize Window', id: 'desktop-minimize' },
  { title: 'Maximize Window', id: 'desktop-maximize' },
  { title: 'Close Window', id: 'desktop-close_window' },
  { title: 'Desktop Screenshot', id: 'desktop-screenshot' },
  { title: 'Click Image', id: 'desktop-image_click' },
  { title: 'Wait for Image', id: 'desktop-wait_image' },
  { title: 'Copy to Clipboard', id: 'desktop-clipboard_copy' },
]

# Desktop Automation

Components for desktop application automation. Control Windows applications, manage windows, simulate keyboard and mouse. {{ className: 'lead' }}

<Note>
  This category contains **12 components**. See also: [Web Automation](/components/web) · [File Operations](/components/files) · [Excel](/components/excel)
</Note>

## Common Use Cases

- Automate legacy desktop apps
- Control SAP, Oracle, or custom apps
- Interact with Windows dialogs
- Manage multiple windows

---

## Open Application {{ id: 'desktop-open_app' }}

<NodeHeader icon={`
<svg
  class="lucide lucide-app-window"
  xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <rect x="2" y="4" width="20" height="16" rx="2" />
  <path d="M10 4v4" />
  <path d="M2 8h20" />
  <path d="M6 4v4" />
</svg>
`} label="Open Application" description="Launch an application" bgColor="#e0e7ff" textColor="#4338ca" />

<Properties>
  <Property name="type" type="string">
    `desktop.open_app`
  </Property>
</Properties>

### When to Use

- Launch SAP GUI for enterprise automation
- Open legacy Windows applications
- Start desktop-based accounting software
- Run custom in-house desktop tools

### Configuration

<Properties>
  <Property name="path" type="text" required>
    Application Path
  </Property>
  <Property name="args" type="text">
    Arguments
  </Property>
</Properties>

### Example

```json
{
  "id": "node-1",
  "type": "desktop.open_app",
  "label": "Open Application",
  "config": {
    "path": "your-path",
    "args": "your-args"
  },
  "outputs": {
    "success": "next-node",
    "error": "error-handler"
  }
}
```

---

## Desktop Click {{ id: 'desktop-click' }}

<NodeHeader icon={`
<svg
  class="lucide lucide-mouse-pointer"
  xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <path d="M12.586 12.586 19 19" />
  <path d="M3.688 3.037a.497.497 0 0 0-.651.651l6.5 15.999a.501.501 0 0 0 .947-.062l1.569-6.083a2 2 0 0 1 1.448-1.479l6.124-1.579a.5.5 0 0 0 .063-.947z" />
</svg>
`} label="Desktop Click" description="Click at coordinates or element" bgColor="#e0e7ff" textColor="#4338ca" />

<Properties>
  <Property name="type" type="string">
    `desktop.click`
  </Property>
</Properties>

### When to Use

- Click buttons in desktop applications
- Select items in list views
- Interact with ribbon menus
- Click toolbar icons

### Configuration

<Properties>
  <Property name="locator" type="text">
    Element Locator (e.g., `name:Button1`)
  </Property>
  <Property name="x" type="number">
    X Coordinate
  </Property>
  <Property name="y" type="number">
    Y Coordinate
  </Property>
</Properties>

### Example

```json
{
  "id": "node-1",
  "type": "desktop.click",
  "label": "Desktop Click",
  "config": {
    "locator": "name:Button1",
    "x": 0,
    "y": 0
  },
  "outputs": {
    "success": "next-node",
    "error": "error-handler"
  }
}
```

---

## Type Text {{ id: 'desktop-type_text' }}

<NodeHeader icon={`
<svg
  class="lucide lucide-keyboard"
  xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <path d="M10 8h.01" />
  <path d="M12 12h.01" />
  <path d="M14 8h.01" />
  <path d="M16 12h.01" />
  <path d="M18 8h.01" />
  <path d="M6 8h.01" />
  <path d="M7 16h10" />
  <path d="M8 12h.01" />
  <rect width="20" height="16" x="2" y="4" rx="2" />
</svg>
`} label="Type Text" description="Type text with keyboard" bgColor="#e0e7ff" textColor="#4338ca" />

<Properties>
  <Property name="type" type="string">
    `desktop.type_text`
  </Property>
</Properties>

### When to Use

- Enter data into text fields in desktop apps
- Fill forms in legacy systems
- Input search queries in desktop search bars
- Type commands in terminal applications

### Configuration

<Properties>
  <Property name="text" type="text" required>
    Text
  </Property>
  <Property name="interval" type="number">
    Key Interval (ms) Default: `50`
  </Property>
</Properties>

### Example

```json
{
  "id": "node-1",
  "type": "desktop.type_text",
  "label": "Type Text",
  "config": {
    "text": "your-text",
    "interval": "50"
  },
  "outputs": {
    "success": "next-node",
    "error": "error-handler"
  }
}
```

---

## Send Hotkey {{ id: 'desktop-hotkey' }}

<NodeHeader icon={`
<svg
  class="lucide lucide-command"
  xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <path d="M15 6v12a3 3 0 1 0 3-3H6a3 3 0 1 0 3 3V6a3 3 0 1 0-3 3h12a3 3 0 1 0-3-3" />
</svg>
`} label="Send Hotkey" description="Press keyboard shortcut" bgColor="#e0e7ff" textColor="#4338ca" />

<Properties>
  <Property name="type" type="string">
    `desktop.hotkey`
  </Property>
</Properties>

### When to Use

- Use Ctrl+C/Ctrl+V for copy/paste operations
- Press Enter to submit forms
- Use Alt+Tab to switch windows
- Trigger keyboard shortcuts for menu actions

### Configuration

<Properties>
  <Property name="keys" type="text" required>
    Key Combination (e.g., `ctrl+c`)
  </Property>
</Properties>

### Example

```json
{
  "id": "node-1",
  "type": "desktop.hotkey",
  "label": "Send Hotkey",
  "config": {
    "keys": "ctrl+c"
  },
  "outputs": {
    "success": "next-node",
    "error": "error-handler"
  }
}
```

---

## Get Window {{ id: 'desktop-get_window' }}

<NodeHeader icon={`
<svg
  class="lucide lucide-square"
  xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <rect width="18" height="18" x="3" y="3" rx="2" />
</svg>
`} label="Get Window" description="Find and focus window" bgColor="#e0e7ff" textColor="#4338ca" />

<Properties>
  <Property name="type" type="string">
    `desktop.get_window`
  </Property>
</Properties>

### When to Use

- Find and focus specific application windows
- Handle multiple instances of the same app
- Wait for application windows to appear
- Get window state for conditional logic

### Configuration

<Properties>
  <Property name="title" type="text" required>
    Window Title
  </Property>
  <Property name="partial_match" type="boolean">
    Partial Match Default: `true`
  </Property>
</Properties>

### Example

```json
{
  "id": "node-1",
  "type": "desktop.get_window",
  "label": "Get Window",
  "config": {
    "title": "your-title",
    "partial_match": "true"
  },
  "outputs": {
    "success": "next-node",
    "error": "error-handler"
  }
}
```

---

## Minimize Window {{ id: 'desktop-minimize' }}

<NodeHeader icon={`
<svg
  class="lucide lucide-minus"
  xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <path d="M5 12h14" />
</svg>
`} label="Minimize Window" description="Minimize active window" bgColor="#e0e7ff" textColor="#4338ca" />

<Properties>
  <Property name="type" type="string">
    `desktop.minimize`
  </Property>
</Properties>

### When to Use

- Minimize distracting windows during automation
- Clean up screen for screenshots
- Reduce interference with user activities
- Manage desktop real estate

### Example

```json
{
  "id": "node-1",
  "type": "desktop.minimize",
  "label": "Minimize Window",
  "config": {
    // No configuration required
  },
  "outputs": {
    "success": "next-node",
    "error": "error-handler"
  }
}
```

---

## Maximize Window {{ id: 'desktop-maximize' }}

<NodeHeader icon={`
<svg
  class="lucide lucide-maximize-2"
  xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <path d="M15 3h6v6" />
  <path d="m21 3-7 7" />
  <path d="m3 21 7-7" />
  <path d="M9 21H3v-6" />
</svg>
`} label="Maximize Window" description="Maximize active window" bgColor="#e0e7ff" textColor="#4338ca" />

<Properties>
  <Property name="type" type="string">
    `desktop.maximize`
  </Property>
</Properties>

### When to Use

- Ensure consistent UI element positions
- Prepare window for full-screen screenshots
- Maximize data grid visibility
- Standardize window state for image-based automation

### Example

```json
{
  "id": "node-1",
  "type": "desktop.maximize",
  "label": "Maximize Window",
  "config": {
    // No configuration required
  },
  "outputs": {
    "success": "next-node",
    "error": "error-handler"
  }
}
```

---

## Close Window {{ id: 'desktop-close_window' }}

<NodeHeader icon={`
<svg
  class="lucide lucide-square-x"
  xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <rect width="18" height="18" x="3" y="3" rx="2" ry="2" />
  <path d="m15 9-6 6" />
  <path d="m9 9 6 6" />
</svg>
`} label="Close Window" description="Close active window" bgColor="#e0e7ff" textColor="#4338ca" />

<Properties>
  <Property name="type" type="string">
    `desktop.close_window`
  </Property>
</Properties>

### When to Use

- Close dialog boxes after confirmation
- Clean up temporary windows
- Exit applications gracefully
- Handle unexpected popup windows

### Example

```json
{
  "id": "node-1",
  "type": "desktop.close_window",
  "label": "Close Window",
  "config": {
    // No configuration required
  },
  "outputs": {
    "success": "next-node",
    "error": "error-handler"
  }
}
```

---

## Desktop Screenshot {{ id: 'desktop-screenshot' }}

<NodeHeader icon={`
<svg
  class="lucide lucide-monitor"
  xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <rect width="20" height="14" x="2" y="3" rx="2" />
  <line x1="8" x2="16" y1="21" y2="21" />
  <line x1="12" x2="12" y1="17" y2="21" />
</svg>
`} label="Desktop Screenshot" description="Capture screen region" bgColor="#e0e7ff" textColor="#4338ca" />

<Properties>
  <Property name="type" type="string">
    `desktop.screenshot`
  </Property>
</Properties>

### When to Use

- Capture desktop state for audit trails
- Debug failed automation steps
- Document completed transactions
- Create training materials

### Configuration

<Properties>
  <Property name="path" type="text" required>
    Save Path
  </Property>
  <Property name="region" type="text">
    Region (x,y,w,h) (e.g., `0,0,1920,1080`)
  </Property>
</Properties>

### Example

```json
{
  "id": "node-1",
  "type": "desktop.screenshot",
  "label": "Desktop Screenshot",
  "config": {
    "path": "your-path",
    "region": "0,0,1920,1080"
  },
  "outputs": {
    "success": "next-node",
    "error": "error-handler"
  }
}
```

---

## Click Image {{ id: 'desktop-image_click' }}

<NodeHeader icon={`
<svg
  class="lucide lucide-image"
  xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <rect width="18" height="18" x="3" y="3" rx="2" ry="2" />
  <circle cx="9" cy="9" r="2" />
  <path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21" />
</svg>
`} label="Click Image" description="Find and click image on screen" bgColor="#e0e7ff" textColor="#4338ca" />

<Properties>
  <Property name="type" type="string">
    `desktop.image_click`
  </Property>
</Properties>

### When to Use

- Click buttons identified by image in Citrix/VDI
- Interact with non-standard UI controls
- Handle applications without accessible UI elements
- Automate remote desktop sessions

### Configuration

<Properties>
  <Property name="image_path" type="text" required>
    Image Path
  </Property>
  <Property name="confidence" type="number">
    Confidence (0-1) Default: `0`
  </Property>
</Properties>

### Example

```json
{
  "id": "node-1",
  "type": "desktop.image_click",
  "label": "Click Image",
  "config": {
    "image_path": "your-image_path",
    "confidence": "0"
  },
  "outputs": {
    "success": "next-node",
    "error": "error-handler"
  }
}
```

---

## Wait for Image {{ id: 'desktop-wait_image' }}

<NodeHeader icon={`
<svg
  class="lucide lucide-scan-search"
  xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <path d="M3 7V5a2 2 0 0 1 2-2h2" />
  <path d="M17 3h2a2 2 0 0 1 2 2v2" />
  <path d="M21 17v2a2 2 0 0 1-2 2h-2" />
  <path d="M7 21H5a2 2 0 0 1-2-2v-2" />
  <circle cx="12" cy="12" r="3" />
  <path d="m16 16-1.9-1.9" />
</svg>
`} label="Wait for Image" description="Wait for image to appear" bgColor="#e0e7ff" textColor="#4338ca" />

<Properties>
  <Property name="type" type="string">
    `desktop.wait_image`
  </Property>
</Properties>

### When to Use

- Wait for application to fully load
- Detect when dialogs appear
- Handle variable loading times
- Ensure UI is ready before interaction

### Configuration

<Properties>
  <Property name="image_path" type="text" required>
    Image Path
  </Property>
  <Property name="timeout" type="number">
    Timeout (ms) Default: `30000`
  </Property>
</Properties>

### Example

```json
{
  "id": "node-1",
  "type": "desktop.wait_image",
  "label": "Wait for Image",
  "config": {
    "image_path": "your-image_path",
    "timeout": "30000"
  },
  "outputs": {
    "success": "next-node",
    "error": "error-handler"
  }
}
```

---

## Copy to Clipboard {{ id: 'desktop-clipboard_copy' }}

<NodeHeader icon={`
<svg
  class="lucide lucide-copy"
  xmlns="http://www.w3.org/2000/svg"
  width="24"
  height="24"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <rect width="14" height="14" x="8" y="8" rx="2" ry="2" />
  <path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2" />
</svg>
`} label="Copy to Clipboard" description="Copy text to clipboard" bgColor="#e0e7ff" textColor="#4338ca" />

<Properties>
  <Property name="type" type="string">
    `desktop.clipboard_copy`
  </Property>
</Properties>

### When to Use

- Extract data via copy/paste from locked fields
- Transfer data between applications
- Work around read-only text fields
- Bulk copy data from desktop apps

### Configuration

<Properties>
  <Property name="text" type="text" required>
    Text
  </Property>
</Properties>

### Example

```json
{
  "id": "node-1",
  "type": "desktop.clipboard_copy",
  "label": "Copy to Clipboard",
  "config": {
    "text": "your-text"
  },
  "outputs": {
    "success": "next-node",
    "error": "error-handler"
  }
}
```

---

