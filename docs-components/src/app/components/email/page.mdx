export const metadata = {
  title: 'Email',
  description: 'Complete email automation. Send, read, reply, forward emails via SMTP, IMAP, Outlook, or Gmail.',
}

export const sections = [
  { title: 'Send Email', id: 'email-send' },
  { title: 'Read Emails', id: 'email-read' },
  { title: 'Reply to Email', id: 'email-reply' },
  { title: 'Forward Email', id: 'email-forward' },
  { title: 'Download Attachment', id: 'email-download_attachment' },
  { title: 'Move Email', id: 'email-move' },
  { title: 'Delete Email', id: 'email-delete' },
  { title: 'Mark as Read', id: 'email-mark_read' },
  { title: 'Search Emails', id: 'email-search' },
  { title: 'Send via SMTP', id: 'email-send_smtp' },
  { title: 'Send via Outlook', id: 'email-send_outlook' },
  { title: 'Send via Gmail', id: 'email-send_gmail' },
  { title: 'Read via IMAP', id: 'email-read_imap' },
  { title: 'Create Draft', id: 'email-create_draft' },
  { title: 'Get Folders', id: 'email-get_folders' },
]

Complete email automation. Send, read, reply, forward emails via SMTP, IMAP, Outlook, or Gmail API. {{ className: 'lead' }}

<Note>
  For Microsoft 365 (Exchange Online), see the [MS365](/components/ms365) category which offers deeper integration.
</Note>

---

<NodeHeader id="email-send" icon="Send" label="Send Email" description="Send an email" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.send`
  </Property>
</Properties>

**Configuration:**
- **to** (`text`, required) — Recipient email address(es)
- **subject** (`text`, required) — Email subject
- **body** (`textarea`, required) — Email body
- **html** (`boolean`, default: `false`) — Send as HTML

---

<NodeHeader id="email-read" icon="MailOpen" label="Read Emails" description="Read emails from mailbox" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.read`
  </Property>
</Properties>

**Configuration:**
- **folder** (`text`, default: `INBOX`) — Mailbox folder
- **filter** (`text`) — Subject filter
- **unread_only** (`boolean`, default: `true`) — Only fetch unread emails
- **limit** (`number`, default: `10`) — Maximum emails to retrieve

---

<NodeHeader id="email-reply" icon="Reply" label="Reply to Email" description="Reply to an email" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.reply`
  </Property>
</Properties>

**Configuration:**
- **message_id** (`text`, required) — Original message ID
- **body** (`textarea`, required) — Reply body

---

<NodeHeader id="email-forward" icon="Forward" label="Forward Email" description="Forward an email" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.forward`
  </Property>
</Properties>

**Configuration:**
- **message_id** (`text`, required) — Original message ID
- **to** (`text`, required) — Forward to address

---

<NodeHeader id="email-download_attachment" icon="Paperclip" label="Download Attachment" description="Save email attachment" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.download_attachment`
  </Property>
</Properties>

**Configuration:**
- **message_id** (`text`, required) — Message ID
- **save_path** (`text`, required) — Save path

---

<NodeHeader id="email-move" icon="FolderInput" label="Move Email" description="Move email to folder" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.move`
  </Property>
</Properties>

**Configuration:**
- **message_id** (`text`, required) — Message ID
- **folder** (`text`, required) — Target folder

---

<NodeHeader id="email-delete" icon="Trash2" label="Delete Email" description="Delete an email" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.delete`
  </Property>
</Properties>

**Configuration:**
- **message_id** (`text`, required) — Message ID to delete

---

<NodeHeader id="email-mark_read" icon="CheckCircle" label="Mark as Read" description="Mark email as read/unread" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.mark_read`
  </Property>
</Properties>

**Configuration:**
- **message_id** (`text`, required) — Message ID
- **read** (`boolean`, default: `true`) — Mark as read

---

<NodeHeader id="email-search" icon="Search" label="Search Emails" description="Search emails with criteria" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.search`
  </Property>
</Properties>

**Configuration:**
- **query** (`text`, required) — Search query
- **folder** (`text`, default: `INBOX`) — Folder to search

---

<NodeHeader id="email-send_smtp" icon="Server" label="Send via SMTP" description="Send email via SMTP server" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.send_smtp`
  </Property>
</Properties>

**Configuration:**
- **smtp_host** (`text`, required) — SMTP server address
- **port** (`number`, default: `587`) — SMTP port
- **username** (`text`, required) — SMTP username
- **password** (`password`, required) — SMTP password (use `${VAULT.smtp_password}`)
- **tls** (`boolean`, default: `true`) — Use TLS
- **to** (`text`, required) — Recipient
- **subject** (`text`, required) — Subject
- **body** (`textarea`, required) — Body

---

<NodeHeader id="email-send_outlook" icon="Mail" label="Send via Outlook" description="Send email via Outlook" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.send_outlook`
  </Property>
</Properties>

**Configuration:**
- **to** (`text`, required) — Recipient
- **cc** (`text`) — CC recipients
- **bcc** (`text`) — BCC recipients
- **subject** (`text`, required) — Subject
- **body** (`textarea`, required) — Body
- **html** (`boolean`, default: `false`) — HTML body
- **attachments** (`textarea`) — Attachment paths

---

<NodeHeader id="email-send_gmail" icon="Mail" label="Send via Gmail" description="Send email via Gmail API" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.send_gmail`
  </Property>
</Properties>

**Configuration:**
- **to** (`text`, required) — Recipient
- **subject** (`text`, required) — Subject
- **body** (`textarea`, required) — Body
- **html** (`boolean`, default: `false`) — HTML body

---

<NodeHeader id="email-read_imap" icon="Inbox" label="Read via IMAP" description="Read emails via IMAP" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.read_imap`
  </Property>
</Properties>

**Configuration:**
- **imap_host** (`text`, required) — IMAP server address
- **port** (`number`, default: `993`) — IMAP port
- **username** (`text`, required) — IMAP username
- **password** (`password`, required) — IMAP password (use `${VAULT.imap_password}`)
- **ssl** (`boolean`, default: `true`) — Use SSL
- **folder** (`text`, default: `INBOX`) — Folder
- **limit** (`number`, default: `10`) — Max emails

---

<NodeHeader id="email-create_draft" icon="FileEdit" label="Create Draft" description="Create email draft" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.create_draft`
  </Property>
</Properties>

**Configuration:**
- **to** (`text`) — Recipient
- **subject** (`text`) — Subject
- **body** (`textarea`) — Body

---

<NodeHeader id="email-get_folders" icon="FolderTree" label="Get Folders" description="List email folders" bgColor="#fef3c7" textColor="#92400e" />

<Properties>
  <Property name="type" type="string">
    `email.get_folders`
  </Property>
</Properties>

**Output:**
- **folders** (`array`) — List of folder names

---

